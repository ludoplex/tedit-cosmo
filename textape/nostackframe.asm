; ============================================================================
; Leaf procedure (no stack frame, uses only volatile registers)
; ============================================================================

proc_name proc
    ; Your code here (max 4 params in rcx, rdx, r8, r9)
    
    ret
proc_name endp

